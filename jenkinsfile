pipeline {
   agent any

   stages {
        stage('Verify Branch') {
         steps {
            echo "$GIT_BRANCH"
            }
         }
         
         stage('Test Stage') {
         steps {
            echo "$GIT_BRANCH"
            }
         }
         
         stage('Build Stage') {
         steps {
            echo "$GIT_BRANCH"
            }
         }
    }
 }  

